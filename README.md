# juandaescobarr.com — v5

Versión de un solo archivo para GitHub Pages.

## Publicación
Sube a la raíz del repositorio únicamente:

- `index.html`
- `CNAME`
- `.nojekyll`

Las imágenes y ambos CV están incrustados en `index.html`; no existe dependencia de la carpeta `assets`.
